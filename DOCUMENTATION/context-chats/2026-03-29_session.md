# Chat Session — 2026-03-29

## Время: вечер (продолжение дневной сессии)

---

## Выполненные задачи

### 1. Миграция StoryboardPanel на /workspace
- Создан `StoryboardShared.tsx` — извлечены общие компоненты: `SceneBibleBubble`, `BibleEntryCard`, `EditableDuration`, `generateBibleImageFromModal`
- `/workspace/page.tsx` — StoryboardPanel как полностраничный layout
- Удалён StoryboardPanel из `ScriptWriterOverlay.tsx` — убраны panel state, storyboard button, sliding layout
- Обновлён `ShotStudio.tsx` — импорт `SceneBibleBubble` из нового пути

### 2. Навигация — новый GlobalNav
- Удалена кнопка "Studio" (toggle studioPanelRequested)
- Новый порядок: **MEDIA | SCRIPTWRITER | STORYBOARD | TIMELINE | EXPORT**
- Стиль: заглавные буквы, `font-semibold`, `tracking-[0.14em]`
- Groove-разделители (вдавленные вертикальные линии)
- Gradient фон + backdrop-blur, gold glow на активном табе
- KOZA логотип слева приглушённый
- Навбар сверху (56px)

### 3. Автооткрытие Script editor
- `editorMode` в Canvas.tsx инициализируется через lazy initializer
- Если в скрипте есть контент (title !== "UNTITLED" или blocks > 1) — сразу `active: true`
- Убрано мелькание канвы при переходе на Script

### 4. Удаление Pipeline с канвы
- Удалены: Pipeline кнопка, `handleTogglePipeline`, `rebuildPipelineGraph`, `handleRunBreakdown`, `handleGenerateImage`, все pipeline effects
- Удалены: SceneNode, ShotNode, BreakdownNode, ImageGenNode, PreviewNode, BibleNode, SettingsNode импорты
- Удалены: MiniMap, handleSelectionChange, canvasConfigOpen panel
- Канва теперь содержит только ScriptDoc ноду + логотип

### 5. Упрощение тулбара StoryboardPanel
- Убраны: Bible, Timeline, Collapse/Expand, More (...), "Scene Engine" лейбл, "Model:" текст
- Оставлены: название сцены, Breakdown, Fincher, Model selector, Style picker, Config (шестерёнка), Close (X)
- Split Screen перенесён из табов в правый угол тулбара
- Все размеры увеличены на ~20%

### 6. Титульная страница сценария — стандарт
- Шрифт: Courier New (стандарт киносценария)
- Название: по центру, ~33% от верха, bold
- "Written by" / Автор: по центру, ~40%
- Черновик + Дата: внизу слева (left: 108px, bottom: 72px)
- line-height: 2 (двойной интервал)

### 7. Фикс ScriptViewer "A A" кнопок
- Убран `sticky top-0` который перекрывал текст полосой
- Заменён на `absolute right-4 top-3` — плавающие кнопки в углу

### 8. Dock Magnification эффект (macOS-style)
- Компонент `DockStrip` в ShotStudio
- Гауссова кривая масштабирования при hover
- Base: 48px, Max: 60px (1.25x), Radius: 80px
- `align-items: flex-end` — рост вверх
- Кроссплатформенный (CSS transform + JS mousemove)

### 9. Сохранение generationHistory после reload
- `partialize` теперь сохраняет entries с `blobKey` (раньше стирал всё в `[]`)
- `onRehydrateStorage` восстанавливает URL каждой записи из IndexedDB через `loadBlob`
- DockStrip показывается при `history.length >= 1`

### 10. Library page (/library = Media)
- Полноценная страница вместо заглушки
- Grid с превью, поиск, Upload, drag-and-drop
- Разделение на Images / Files
- Лайтбокс по клику, удаление с hover

### 11. Фикс SceneSync на /workspace
- Добавлен `useSceneSync()` в workspace page
- Director Cut теперь показывает сцены после reload

### 12. Фикс стилей в промптах
- `stripStyleFromPrompt()` в `promptBuilder.ts` — удаляет все стилевые пресеты перед добавлением нового
- Убирает "Photorealistic" и другие leaked стили из `shot.imagePrompt`

### 13. Отдельный стиль для библии
- `bibleStyle` в board store (независим от `projectStyle`)
- Style picker в SceneBibleBubble: Cinematic, Realistic, Film Noir, Anime, Sketch, Watercolor
- Кнопка "Generate All (N)" — генерирует все элементы без изображений

### 14. Улучшенный Smart Scan
- Отправляет LLM полный список существующих элементов с описаниями
- Обнаружение синонимов (машина = автомобиль)
- Авто-заполнение пустых `appearancePrompt` по контексту сцены
- Двухзадачный формат: `newProps` + `updatedDescriptions`

### 15. Edit Mode в ShotStudio
- Transform state: flipH, flipV, rotate (0/90/180/270)
- CSS transform на изображении в реальном времени
- Edit mode bar: Flip H/V (toggle), Rotate CW/CCW, Cancel, Save
- Save — canvas рендер с трансформами → pushEdit в историю
- В edit mode скрываются все панели, фото увеличивается

### 16. Apple-style Drag & Drop
- Кастомный ghost-image: scale(1.04), rotate(1.5deg), deep shadow, gold border
- Оригинальная карточка: opacity 0.3, scale(0.96)
- Мгновенное раздвигание слотов (убрана задержка 1200ms)
- cubic-bezier(0.22,1,0.36,1) анимации

### 17. Фикс SAM import
- Статический import `onnxruntime-web/wasm` заменён на динамический `import()`
- Убрана ошибка "Invalid URL" при загрузке с Turbopack

### 18. Фикс expanded ref в ShotStudio
- Клик на любое место (изображение, фон) закрывает expanded reference preview
- stopPropagation на кнопках ref strip и expanded preview panel

---

## Нерешённые / в процессе

- **ShotStudio handleGenerate ошибка 500**: `buildPromptWithAI` вызывает `/api/chat` с `claude-opus-4-20250514` — если ANTHROPIC_API_KEY не настроен, падает с "Image generation failed". Нужно: либо fallback на другую модель, либо лучшая обработка ошибки.

---

## Затронутые файлы (ключевые)

| Файл | Действие |
|------|----------|
| `src/components/app/GlobalNav.tsx` | Полная переработка |
| `src/app/layout.tsx` | pt → pb → pt, высота навбара |
| `src/app/workspace/page.tsx` | Создан (StoryboardPanel full-page) |
| `src/app/library/page.tsx` | Переписан (полная библиотека) |
| `src/components/editor/screenplay/StoryboardShared.tsx` | Создан (извлечённые компоненты) |
| `src/components/editor/screenplay/StoryboardPanel.tsx` | Упрощён тулбар, увеличены размеры, drag&drop |
| `src/components/editor/ScriptWriterOverlay.tsx` | Удалён StoryboardPanel, новая титульная |
| `src/components/editor/ShotStudio.tsx` | DockStrip, edit mode, SAM lazy import, ref fix |
| `src/components/editor/screenplay/ScriptViewer.tsx` | Фикс "A A" кнопок |
| `src/components/board/Canvas.tsx` | Удалён pipeline, auto-open editor |
| `src/store/board.ts` | Добавлен bibleStyle |
| `src/store/timeline.ts` | Persist generationHistory |
| `src/lib/promptBuilder.ts` | stripStyleFromPrompt |

---

## Архитектурные решения

1. **StoryboardPanel теперь на /workspace** — изолированный chunk, нет coupling с ScriptWriterOverlay
2. **Навигация DaVinci Resolve style** — 5 вкладок сверху, каждая = отдельная страница
3. **bibleStyle отделён от projectStyle** — библия генерируется в своём стиле
4. **generationHistory сохраняется** — entries с blobKey переживают reload
5. **Smart Scan с контекстом** — LLM знает что уже найдено, не дублирует, дописывает описания
